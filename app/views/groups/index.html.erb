<p id="notice"><%= notice %></p>

<div class="container-fluid mb-12 pt-3 pb-1 bg-primary">

 <div class="back-arrow text-left ">
<span style="font-size: 48px; color: Dodgerblue;">
 <i class="fas fa-arrow-left"></i>
</span>
        
    </div>
    <div class="head-login text-center ">
        <h3><i class="fas fa-gift"><strong>Groups</strong></i></h3>
        <br>
       
    </div>
    
    </div>
<p id="notice"><%= notice %></p>

<div class="container">

  <div class="row g-0 border-bottom">
   <% @groups.each do |group| %>
   <%= link_to  group do %>
    <div class="col-4">
    <%= group.icon %>
    </div>
    <div class="col-8">
      <h5 class="card-title"><%= group.name %><span class="like"></span></h5>
        <p class="card-text"><small class="text-muted"><%= group.created_at.strftime("%b %d, %H:%M, %Y") %></small></p>
    <%end%>
    
      <% end %>
    </div>

  </div>

</div>
<br>
  <div class="row ">
    <div class="col-12">
<%= link_to 'New Group', new_group_path, class: 'btn btn-primary w-100 mt-1'%>
</div>
</div>


<%# <td><%= link_to 'Show', group %></td>
        <%# <td><%= link_to 'Edit', edit_group_path(group) %></td>
        <%# <td><%= link_to 'Destroy', group, method: :delete, data: { confirm: 'Are you sure?' } %></td> 











